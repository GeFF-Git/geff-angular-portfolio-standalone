[build]
  command = "npm ci && npm run build"
  publish = "dist/geff-angular-portfolio-standalone/browser"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--version"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
